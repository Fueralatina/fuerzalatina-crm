'use client'; import { useState, useEffect } from 'react'; import { createClient } from '@supabase/supabase-js'; const supabaseUrl = 'https://itcypwhymlbbxjbdxblt.supabase.co'; const supabaseAnonKey = 'sb_publishable_JoLuw3BYPdojI0wZWr9zfQ_Nok393EK'; // ← your real key is already in env vars, but for safety we'll use env later const supabase = createClient(supabaseUrl, supabaseAnonKey); export default function Clients() { const [clients, setClients] = useState<any[]>([]); const [search, setSearch] = useState(''); const [loading, setLoading] = useState(true); useEffect(() => { fetchClients(); }, []); const fetchClients = async () => { const { data, error } = await supabase .from('clients') .select('*') .order('created_at', { ascending: false }); if (error) { console.error(error); } else { setClients(data || []); } setLoading(false); }; const filteredClients = clients.filter(client => client.full_name?.toLowerCase().includes(search.toLowerCase()) || client.email?.toLowerCase().includes(search.toLowerCase()) || client.policy_number?.includes(search) ); return ( <div style={{ padding: '40px', fontFamily: 'Arial, sans-serif', maxWidth: '1200px', margin: '0 auto' }}> <h1>👥 Client Management</h1> <p>Manage all your insurance clients</p> <div style={{ margin: '20px 0' }}> <input type="text" placeholder="Search by name, email or policy number..." value={search} onChange={(e) => setSearch(e.target.value)} style={{ width: '100%', padding: '12px', fontSize: '16px', border: '1px solid #ccc', borderRadius: '6px' }} /> </div> {loading ? ( <p>Loading clients...</p> ) : ( <div style={{ overflowX: 'auto' }}> <table style={{ width: '100%', borderCollapse: 'collapse', background: 'white' }}> <thead> <tr style={{ background: '#f0f0f0' }}> <th style={{ padding: '12px', textAlign: 'left', border: '1px solid #ddd' }}>Full Name</th> <th style={{ padding: '12px', textAlign: 'left', border: '1px solid #ddd' }}>Email</th> <th style={{ padding: '12px', textAlign: 'left', border: '1px solid #ddd' }}>Phone</th> <th style={{ padding: '12px', textAlign: 'left', border: '1px solid #ddd' }}>Policy Number</th> <th style={{ padding: '12px', textAlign: 'left', border: '1px solid #ddd' }}>LOB</th> <th style={{ padding: '12px', textAlign: 'left', border: '1px solid #ddd' }}>Renewal Date</th> </tr> </thead> <tbody> {filteredClients.length === 0 ? ( <tr> <td colSpan={6} style={{ padding: '20px', textAlign: 'center' }}> No clients found. </td> </tr> ) : ( filteredClients.map((client) => ( <tr key={client.id}> <td style={{ padding: '12px', border: '1px solid #ddd' }}>{client.full_name}</td> <td style={{ padding: '12px', border: '1px solid #ddd' }}>{client.email}</td> <td style={{ padding: '12px', border: '1px solid #ddd' }}>{client.phone}</td> <td style={{ padding: '12px', border: '1px solid #ddd' }}>{client.policy_number}</td> <td style={{ padding: '12px', border: '1px solid #ddd' }}>{client.line_of_business}</td> <td style={{ padding: '12px', border: '1px solid #ddd' }}> {client.renewal_date ? new Date(client.renewal_date).toLocaleDateString() : '-'} </td> </tr> )) )} </tbody> </table> </div> )} <div style={{ marginTop: '40px' }}> <button onClick={() => window.location.href = '/'} style={{ padding: '12px 24px', background: '#0070f3', color: 'white', border: 'none', borderRadius: '6px', cursor: 'pointer' }} > ← Back to Dashboard </button> </div> </div> ); }
Wave AI Assistant

Tell me more about it

Show me related topics

Start prompt with selection
